#!/bin/sh

# $1 is the directory where the app is being built

BUILD_DIR=$1

echo "-----> Creating process types"

echo "---"
echo "default_process_types:"
echo "  web: $BUILD_DIR/grafana-agent-linux-amd64 -config.file=$BUILD_DIR/agent.yaml"
