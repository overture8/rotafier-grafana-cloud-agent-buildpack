#!/bin/sh

# $1 is the directory where the app is being built

BUILD_DIR=$1

echo "-----> Creating process types"

# Create the Procfile
echo "web: $BUILD_DIR/agent-linux-amd64 -config.file=$BUILD_DIR/agent.yaml" > $BUILD_DIR/Procfile
